def binsear(n,l, low=0, high=len(l)-1):
    
    if len(l)==:
        return
    if n==l[len(l)//2]:
        return(len()//2)
    elif n>=l[len(l)//2]:
        return(binsear(n,l[len(l)//2:])
    elif n<=l[len(l)//2]:
        return(binsear(n,l[:(len(l)//2)+1])
        